---
schema_version: 1
design_id: plugin-1788049717130-59100204
kind: plugin
name: obs-bridge
status: accepted
target_path: "C:\\Users\\rizek\\plugins\\obs-bridge"
accepted_at: 2026-08-30T00:28:37.129Z
authority_build: "codex-cli 0.149.0 / codex-runtime 0.1.8 / bridge-runtime 0.1.1"
source_digest: 4e5a4b96249c99abbfde0f66858617ea7fd3bc669675a73c437f67b566d93cbc
marketplace: personal
interface_asset_profile: personal-png-v1
mcp_disposition: bundled-server
open_questions: []
---

# Intent and non-goals

OBS Bridge 0.1.2 preserves the four-Skill, three-tool reversible obs-websocket 5.x boundary while adopting Bridge Runtime profile v1.1, clean Git provenance, typed certification commands, and issuer-bound evidence. OBS remains authoritative for scenes, inputs, and runtime state; the bridge retains admission, pre-state, receipts, readback, and rollback authority.

No product workflow, raw RPC passthrough, UI automation, output/stream/record control, credential persistence, compiled OBS plugin, cloud service, or shared Bridge Runtime dependency is added.

# Complete package tree

Retain the complete 0.1.1 package: manifest, `.mcp.json`, AGENTS, metadata/assets, accepted design/audit, four Skills, three references, four MCP modules, four tests, README, package metadata, and bridge profile. Add `.gitignore` and one source-owned real native canary script; modify version-bearing metadata, accepted design, AGENTS/profile lifecycle text, profile v1.1, assertions, audit report, and local Git history.

# Inputs, outputs, persistence, authority, and provenance

The `obs_bridge` server retains `inspect`, `apply_scene_plan`, and `rollback_receipt`. Existing authenticated connection, closed ensure-scene/input plans, conflict refusal, independent readback, automatic rollback, receipt immutability, secret redaction, and foreign-resource preservation remain unchanged.

`bridge-profile/v1.1` binds both `obs64.exe` and `obs-websocket.dll` as application identity artifacts, clean Git commit, fixed source digest policy, typed `npm test` and `npm run test:live` commands, all five tiers, exact receipt schema, personal marketplace, and fresh-task discovery. Existing state under `$CODEX_HOME/state/plugins/obs-bridge/v1` is preserved.

# Activation and verification

Canonical source is `C:\Users\rizek\plugins\obs-bridge`, marketplace identity `obs-bridge@personal`, interface profile `personal-png-v1`, and MCP disposition `bundled-server`. Version becomes 0.1.2. Build/audit through Codex Runtime 0.1.8, certify/promote through Bridge Runtime 0.1.1, update, and verify four Skills plus three tools in a genuinely fresh task.

# Acceptance tests

- Preserve four Skills, three tools, protocol admission, effect/readback, receipt, rollback, and secret-handling contracts.
- Pass package, protocol, workflow, and owned mock tests plus the real OBS 32.2.1 reversible canary.
- Bind clean tagged Git, exact OBS executable and obs-websocket DLL hashes/version, hardened audit, live receipt/readback, exact native restoration, and fresh discovery into v1.1 evidence/certificate.
- Preserve prior receipts, OBS state, and legacy certificates.

# Intentional absences

No new Skill/tool/action family, remote repository, product behavior, raw request surface, UI automation, recording/stream control, cloud service, automatic state purge, or legacy certificate rewrite is introduced.
