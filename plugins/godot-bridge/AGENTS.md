# Godot Bridge operating contract

This directory is the canonical source for `godot-bridge@personal`. Read [the accepted design](design/plugin.md) before material changes, then inspect [the bridge profile](bridge/profile.json), `.codex-plugin/plugin.json`, `.mcp.json`, all seven Skills, their linked references, schemas, MCP modules, Godot addon, and tests.

Source is distinct from the personal marketplace entry, installed cache, active task discovery, enrolled Godot projects, running Godot processes, and state under `$CODEX_HOME/state/plugins/godot-bridge/v1`.

Build with Node 24 and the pinned Godot 4.7.1 stable executable. Run `npm test`, `npm run test:admission`, and `npm run test:live`; render/check `personal-png-v1` assets through Codex Runtime; run the Codex Runtime 0.1.8 blocking audit; persist Bridge Runtime 0.2.0 evidence and certify all six v1.2 tiers; pass promotion for both declared marketplaces; then verify each marketplace snapshot, installation cache, and a genuinely fresh task independently. Never treat mock or command-exit success as a native Godot effect.

The bridge owns generic typed Godot operations and verified single-artifact export only. Game concepts, preset policy, packaging, and distribution belong in separate product plugins. Preserve old receipts, export receipts, checkpoints, and enrollment ownership across updates. Never replace or remove an enrolled addon unless every owned file still matches its recorded hash.

Recovery order: inspect exact source and Git state, API-admission contract, evidence and immutable certificate history, active Codex and Godot versions, application and control-surface identity artifacts, accepted design and audit, both marketplace registrations, installed caches and enabled state; then use a fresh task to verify seven Skills, fourteen MCP tools, and one harmless native inspection.
