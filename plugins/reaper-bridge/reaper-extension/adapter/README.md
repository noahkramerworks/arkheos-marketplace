# Reverse-polling extension adapter

The in-application extension reverse-polls an authenticated ephemeral loopback coordinator and executes admitted operations on the application's main thread. Expose only pairing diagnostics and disconnect controls.
