# Recovery

If disconnected, ensure the coordinator is running, the exact DLL is installed, and REAPER was restarted. If a runtime file is stale, stop the owning MCP process; do not reuse its token. If rollback cannot prove exact bytes, preserve receipt, checkpoint, current project, and failure evidence and require manual recovery from a copy.

