# ArkheOS Krita Bridge extension

This PyKrita 5.3.3 extension reverse-polls an authenticated ephemeral loopback coordinator and executes only the bridge's closed document operations on Krita's application thread. It exposes no menu action, evaluator, command, filter, raw protocol, or unrestricted path surface.
